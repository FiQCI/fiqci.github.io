source "https://rubygems.org"

gem "jekyll", "~> 4.3.4"

# Sitemap generation. Installed on both repos but only activated on the
# production site via the `plugins` list in the generated _config_webpack.yml
# (see webpack.config.js); the dev site is noindex and needs no sitemap.
gem "jekyll-sitemap", "~> 1.4"
